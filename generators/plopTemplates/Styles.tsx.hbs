import styled from 'styled-components';

export const Wrapper = styled.div`
  width: 100%;
  height: auto;
`;

export const ReplaceThisComponent = styled.div`
  ${({ theme }) => `
    width: 100%;
    height: 200px;
    background-color: ${theme.v1.semanticColors.canvas.highlight.brand};
  `}
`;
